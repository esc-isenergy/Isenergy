	@{if (IsEnergyModel.Filters.AuthorizeMeAll.Authorize("Administrators", User.Identity.Name))
                 
   {
       Html.DevExpress().Menu(settings =>
       {
           settings.Name = "HeaderMenu";
           settings.Width = System.Web.UI.WebControls.Unit.Percentage(100);
           settings.Styles.Style.Border.BorderWidth = System.Web.UI.WebControls.Unit.Pixel(0);
           settings.Styles.Style.BorderTop.BorderWidth = System.Web.UI.WebControls.Unit.Pixel(1);

           settings.Items.Add(item =>
           {
               item.Text = "Главная";
               item.NavigateUrl = "~/Home";
           });
           settings.Items.Add(item =>
           {
               item.Text = "Пользователи";
               item.NavigateUrl = "~/Users";
           });
           settings.Items.Add(item =>
           {
               item.Text = "Абоненты";
               item.NavigateUrl = "~/Subscriber";
               item.Items.Add(subItem =>
                {
                    subItem.Text = "Юридическое лицо";
                    subItem.NavigateUrl = "~/LegalEntities";
                });
               item.Items.Add(subItem =>
                {
                    subItem.Text = "Индивидуальный предприниматель";
                    subItem.NavigateUrl = "~/SoleTraders";
                });
               item.Items.Add(subItem =>
                {
                    subItem.Text = " Физическое лицо";
                    subItem.NavigateUrl = "~/PhysicalPersons";
                });

           });
           settings.Items.Add(item =>
           {
               item.Text = "Группы";
               if (IsEnergyModel.Filters.AuthorizeMeAll.Authorize("Administrators.ModifyingAdministrators", User.Identity.Name))
               {
                   item.Items.Add(subitem =>
                           {
                               subitem.Text = "Администраторы";
                               subitem.NavigateUrl = "~/Administrators";
                           });
               } if (IsEnergyModel.Filters.AuthorizeMeAll.Authorize("Administrators.ModifyingOperators", User.Identity.Name))
               {
                   item.Items.Add(subitem =>
                   {
                       subitem.Text = "Операторы";
                       subitem.NavigateUrl = "~/Operators";
                   });
               }
           });
           settings.Items.Add(item =>
           {

               item.Text = "Новости и письма";
               item.Items.Add(subItem =>
               {
                   subItem.Text = "Рассылка писем";
                   subItem.NavigateUrl = "~/EmailDistribution";
               });

           });

       }).GetHtml();
   }
   }