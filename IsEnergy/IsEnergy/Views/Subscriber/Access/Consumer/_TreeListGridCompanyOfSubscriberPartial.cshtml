@{
	var treeList = Html.DevExpress().TreeList(settings => {
        settings.Name = "TreeListGridCompanyOfSubscriber";
        settings.CallbackRouteValues = new { Controller = "Subscriber", Action = "TreeListGridCompanyOfSubscriberPartial" };
		
				
		settings.AutoGenerateColumns = false;

				settings.KeyFieldName = "idSubdivision";
		settings.ParentFieldName = "IdParent";
				settings.Columns.Add(
            column => {
                column.FieldName = "SubdivisionName";
                column.Caption = "Отделение сетевой компании";
            }
        );
						
		settings.SettingsPager.Visible = true;
		settings.SettingsSelection.Enabled = true;
	});
	}
@treeList.Bind(Model).GetHtml()